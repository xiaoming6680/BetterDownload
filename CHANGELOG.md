# BetterDownload 更新记录

## 0.5.0

- 修复：插件读取 `status.json` 时转换程序写入失败，会被当作歌曲失败（已完成的歌曲被卡片误报“未完成”、失败数增加），严重时转换程序直接退出。状态写入改为重试后跳过，不再影响转换。
- 修复：封面格式无法识别、歌曲信息损坏或标签写入失败时整首歌没有输出。现在仍保存完整音频，并在卡片上说明未写入的部分。
- 修复：设置页的错误提示在问题消失后仍一直显示，只能靠开关插件清除。
- 转换程序无法启动（例如被安全软件拦截）或数据目录无法写入时给出提示和需要信任的文件路径，不再无声地反复重启。
- 转换程序改为有任务时才启动、空闲 1 分钟后退出；空闲时界面从每秒读取 4 次状态降到 1–2 秒一次。找不到下载接口时逐步放慢重试。
- 新增“转换已有下载”：手动查找 `VipSongsDownload` 中尚未转换的 NCM 并加入转换。
- 同一首歌重新下载时更新插件此前生成的文件，不再生成“歌曲 (2)”副本；用户原有或改动过的文件仍不覆盖。
- 进度卡片重新设计：专辑封面、按封面主色调色的玻璃、状态色、格式标签、渐变进度条与等待流光、连续下载的本轮计数、带图标的“打开文件夹”；提高浅色主题下的文字对比度。
- 设置页重新设计：品牌图标与版本号、状态面板与开关、功能说明，适配浅色与深色主题。
- 新增进度卡片设置：弹出时机（每首歌 / 仅出错 / 不显示）、样式（标准 / 单行简洁）与停留时间（2 / 4 / 6 秒），可在设置页预览。卡片默认停留时间由 6 秒缩短为 4 秒。
- 设置页加入动效：打开时各区块依次浮现、图标弹入并扫光；开关与分段选择的高亮带弹性滑动；按钮按压反馈、查找时的旋转图标、状态文字更新时淡入、错误提示下落展开。系统开启“减少动态效果”时只保留静态布局。
- 自动清理旧版本留下的运行副本；转换记录只保留输出仍存在的条目。
- 构建改用固定版本的 Roslyn 编译器并启用 `/deterministic`，同一源码编出的 `worker.exe` 逐字节一致，Actions 会核对仓库中的程序；`worker.exe` 改为窗口程序，不会出现控制台窗口。
- 构建脚本改为带 BOM 的 UTF-8，修复 `npm run build` 在 Windows PowerShell 5.1 下解析中文失败。

## 0.4.2

- 更新商店预览图：图标加入打开的锁，插件名和功能说明改为居中布局，增加左右留白，减少横向裁剪对文字的影响。
- 预览图压缩为 960 × 480 JPG；下载转换及界面逻辑保持不变。

## 0.4.1

- 降低卡片底色不透明度与颗粒强度，让背景颜色透过毛玻璃；保持背景模糊和清晰文字。

## 0.4.0 — 发布准备版

- 正式显示名称改为 BetterDownload，作者为 XIAOMING6680。
- 270px 进度卡片改用亚克力材质：背景模糊、半透明着色、柔和高光与细颗粒纹理。
- 调整字体、字重和对比度，支持键盘焦点；保持小型文件夹入口和闲置自动退出。
- 在不支持背景模糊或需要更高对比度的环境使用实色背景。
- 补齐发布检查脚本、源码打包脚本、投稿模板和 Actions 验证流程。
- 保留 `ncm-better-download` 标识和原配置，以便升级已有开发版。

## 0.3.1

- 描述更新为“自动解锁下载的 VIP 歌曲”，明确普通音频无需转换。

## 0.3.0

- 下载完成事件驱动转换，输出至 `VipSongsDownload/unlock`。
- 内嵌封面和歌曲信息，保留原始音频帧与源文件。
- 单按钮启停，移除目录扫描和手动目录设置。

0.4.0 修复：点击打开文件夹后释放按钮焦点；窗口失焦时清理悬停状态，避免卡片无法自动收回。
