# BetterDownload 验证记录

## 0.5.0 自动化验证

2026-09-25：

- 18 项 Node 测试通过。新增覆盖：`status.json` 被读取方占用时转换程序不退出、不误报（同一测试下旧版程序退出）；封面格式无法识别和标签写入失败时仍输出完整音频；手动查找只列出未转换的 NCM；空闲自动退出；重复下载只更新插件自己生成且未被改动的文件；封面副本与格式字段。
- Edge + Playwright 界面回归通过：无任务时不启动转换程序、专辑封面与环境光、等待流光、本轮计数、手动查找、转换程序无响应提示、弹出时机（不显示时静默转换、仅出错时只提示失败）、简洁样式与预览、停留时间（2 秒按时收起）、方向键切换选项、设置页动效（打开时 9 个区块均有入场动画、选项高亮滑动、减少动态效果时停用）、旧运行副本清理、开关、浅色与深色主题、窄窗口。截图与动效逐帧截图已人工检查。
- 同一源码在不同路径下连续构建，`worker.exe` 的 SHA256 一致。
- 尚未在真实网易云客户端中完成 0.5.0 的下载验收；发布前按 [发布流程](PUBLISHING.md) 完成。

## 0.4.2 预览图更新

2026-09-23：仅替换居中构图预览图及版本信息，未修改运行逻辑。预览图为 960 × 480 JPG、34,641 字节。已检查居中方形裁剪，标题及功能文案完整；商店底部信息栏仍可能覆盖图片下部，按作者选定构图发布。实际客户端功能验收记录仍以 0.4.1 为准。

## 0.4.x 自动化验证

- Windows .NET Framework 编译通过，NuGet TagLibSharp 依赖包 SHA256 校验通过。
- 13 项 Node 测试覆盖下载事件、路径限制、FLAC/MP3 音频帧保持、内嵌封面与中文标签、重复任务、同名文件保护、损坏文件、文件锁、取消清理、心跳过期和后台启停。
- 测试音频为原创一秒正弦波；独立剥离输出标签后比对编码帧，与输入一致。
- Edge + Playwright 界面回归覆盖单按钮启停、无目录输入、270px 卡片、小型文件夹入口、自动隐藏、键盘焦点、打开文件夹后的焦点释放、窗口失焦、卸载清理和窄窗口布局。
- 毛玻璃背景为 62% 不透明底色与 28px 模糊；高对比度或减少透明效果时使用实色背景。

## 客户端信息

设置页已在 BetterNCM 1.3.4 正常显示。下载回调接口对照网易云 3.1.37 前端资源核对。

2026-09-08，作者 XIAOMING6680 确认真实下载转换验收成功，转换结果进入 `VipSongsDownload/unlock`，文件管理器可显示标题、歌手和专辑标签。据此将 0.4.1 从预发布转为正式发布。

该确认针对作者本机的实际下载场景。FLAC/MP3 编码帧保持及内嵌封面另有自动化测试覆盖；未据此宣称所有客户端版本、完整播放时长、并发组合和其他插件均已逐项实测。后续版本继续按 [发布流程](PUBLISHING.md) 回归。普通音频无需转换。

## 云端构建

每次 Actions 构建会生成 `build-info.json` 与 `SHA256SUMS.txt`，记录源码提交、构建地址及文件哈希。查看 [Actions](https://github.com/xiaoming6680/BetterDownload/actions) 获取实际构建状态。
